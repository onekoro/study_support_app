$(".places").html("<%= escape_javascript(render partial: "record_places", collection: @places, as: "place") %>");
$(".paginate").html("<%= escape_javascript(render "places/paginate", places: @places) %>");